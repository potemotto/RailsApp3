 <%# 切り取り部分 %>
  <div id="error_explanation">
 <% if book.errors.any? %>
  <h3><%= book.errors.count %> error prohibited this book from being saved:</h3>
  <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  </div>
<% end %>
 <div class="row">
    <div class="col-md-3">

<table class="table">
  <h2>User info</h2>
<tbody>
  <th><%= image_tag current_user.get_profile_image(100,100) %></th>
  <tr>
    <th>name </th>
    <th><%= current_user.name %></th>
  </tr>
  <th>introduction</th><th><%= current_user.introduction %></th>
</tbody>
</table>
<div class="row">
  <li class="btn btn-outline-secondary btn-block">
    <%= link_to  edit_user_path(current_user.id) do %>
   <i class="fas fa-user-cog"></i>
   <% end %>
    </li>
  </div>

<from>
<h2 class="mt-3">New book</h2>
<%= form_with model: book do |f| %>
 <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: "form-control title" %>
  </div>

  <div class="form-group">
        <%= f.label :opinion %>
        <%= f.text_area :body, class: "form-control body" %>
    </div>
    <p> <%= f.submit ' Create Book ' , class: "btn btn-success" %></p>

<% end %>
</from>
<%# ここまでコピー %>
